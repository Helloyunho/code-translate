# Copy this file to .dev.vars and set at least one provider key.
# Optional: set LLM_PROVIDER to openai, gemini, or auto.
# Optional: set provider model names below (backend-only selection).

OPENAI_API_KEY=
GEMINI_API_KEY=
LLM_PROVIDER=auto
OPENAI_MODEL=gpt-5.4
GEMINI_MODEL=gemini-3.1-pro-preview
